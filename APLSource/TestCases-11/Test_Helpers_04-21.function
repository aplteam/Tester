 r←Test_Helpers_04(stopFlag batchFlag);stopFlag;⎕TRAP;list1;list
⍝ Exercise the `ListHelpers` helper.
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')

 r←∆Failed

 list←ListHelpers 1
 →PassesIf 2=≡list
 →PassesIf 2=⍴⍴list

 r←∆OK
